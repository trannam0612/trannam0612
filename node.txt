Note

1. Trả về kết quả cho client
    return res.status(201).json({
            message: 'Thành công',
            data
        })

2. package
- express-promise-router: bắt code thừa,Bỏ code không có gì, nếu không có gì thì next(Error)
- joi: 
- jsonwebtoken: gen token 
-  dotenv:

3. Các phương thức

put: replaceUser

patch: updateUser

4. const user = await User.findById(userID).populate('decks');
- populate: Lấy data qua ref